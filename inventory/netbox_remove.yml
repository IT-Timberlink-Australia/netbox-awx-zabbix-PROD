plugin: netbox.netbox.nb_inventory

config_context: false
strict: false

# Only hosts pending removal
query_filters:
- cf_zbp_status: "remove_pending"

compose:
zbx_status: custom_fields.zbp_status

groups:
status_remove_pending: "{{ zbx_status == 'remove_pending' }}"